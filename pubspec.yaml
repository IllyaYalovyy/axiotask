name: axiotask
description: A native Flutter client for Google Tasks.
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: '>=3.12.0 <3.13.0'
  flutter: '>=3.44.0 <3.45.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: 6.0.0

flutter:
  uses-material-design: true
