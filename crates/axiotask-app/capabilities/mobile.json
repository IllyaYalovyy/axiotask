{
  "identifier": "mobile-oauth",
  "description": "Android-only: deep-link capture of the OAuth redirect and opening the consent URL. Platform-scoped to android so desktop builds (which register neither plugin) never resolve these permissions.",
  "platforms": ["android"],
  "windows": ["main"],
  "permissions": ["deep-link:default", "opener:default"]
}
