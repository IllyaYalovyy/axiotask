{
  "identifier": "mobile-auth",
  "description": "Android-only: Google Play Services sign-in (AuthorizationClient) via the in-repo tauri-plugin-google-auth. Platform-scoped to android so desktop builds (which register no such plugin) never resolve this permission.",
  "platforms": ["android"],
  "windows": ["main"],
  "permissions": ["google-auth:default"]
}
